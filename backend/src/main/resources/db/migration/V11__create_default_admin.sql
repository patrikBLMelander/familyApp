-- Default admin is now created during family registration
-- This migration is kept for reference but will not create admin without a family
-- The admin user is created in FamilyService.registerFamily()

